spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cooking?createDatabaseIfNotExist=true
    username: root
    password: your_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret: your_jwt_secret_key_here_make_it_long_and_secure
  expiration: 86400000 # 24 hours in milliseconds